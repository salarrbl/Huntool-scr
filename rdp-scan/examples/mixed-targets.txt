# Every input form the tool accepts, mixed in one file.
# Use it with an ASN database:  ./rdp-scan examples/mixed-targets.txt --asn-file ip2asn-v4.tsv

# plain CIDR
10.0.0.0/24

# non-canonical base is normalized to the network range
10.0.1.77/30

# single IP
10.0.2.5

# inclusive range
10.0.3.1-10.0.3.9

# wildcard octets (this one is a /16)
10.1.*.*

# AS number (expanded from the ASN database)
AS64512

# hostnames are NOT supported (DNS never happens):
# this line is malformed and will be skipped with a warning:
rdp.example.internal

# bad prefix is skipped with a warning, the rest of the file still scans:
10.0.9.0/33
